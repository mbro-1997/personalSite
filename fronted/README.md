# MBO Personal Site

Vue 3 + Vite personal portfolio site.

## Scripts

```bash
npm install
npm run dev
npm run build
```

For GitHub Pages project deployment, update `base` in `vite.config.js` to the repository path, for example `/personalSite/`.
